import {StyleSheet} from 'react-native';
import {Fonts} from '../../themes';

export default StyleSheet.create({
  containerIcon: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    paddingHorizontal: Fonts.size.size15,
  },
});
